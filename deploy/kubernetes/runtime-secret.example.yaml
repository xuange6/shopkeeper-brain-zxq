apiVersion: v1
kind: Secret
metadata:
  name: shopkeeper-runtime
  namespace: shopkeeper-brain
type: Opaque
stringData:
  LIFECYCLE_DATABASE_URL: postgresql://replace-me
  LIFECYCLE_SCHEDULER_DATABASE_URL: postgresql://direct-or-session-pool-replace-me
  LIFECYCLE_MIGRATION_DATABASE_URL: postgresql://direct-primary-replace-me
  KEDA_DATABASE_URL: postgresql://read-only-queue-metrics-user-replace-me
  LIFECYCLE_ADMIN_TOKEN: replace-me
  ACCESS_CONTEXT_HMAC_SECRET: replace-me
  TASK_STATE_REDIS_URL: rediss://replace-me
  OPENAI_API_KEY: replace-me
  NEO4J_PASSWORD: replace-me
  MINIO_ACCESS_KEY: replace-me
  MINIO_SECRET_KEY: replace-me
  MONGO_URL: mongodb+srv://replace-me
