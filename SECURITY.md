# Security policy

## Reporting a vulnerability

请不要在公开 Issue 中粘贴 API Key、数据库连接串、用户文档或完整生产日志。发现安全问题时，优先通过私下渠道联系维护者，并提供复现步骤、影响范围和建议修复方式。

## 本地部署注意事项

- `knowledge/.env` 只保存在本机，使用 `knowledge/.env.example` 生成配置；
- 如果凭据曾经进入 Git 历史，必须轮换凭据并清理历史，不能只删除工作区文件；
- 生产部署应关闭宽松 CORS，增加认证、限流、审计和 TLS；
- 上传接口只接受白名单文档类型，仍建议在网关层设置更小的请求体上限；
- 知识文档视为不可信输入，回答链路不得执行文档中的指令。
